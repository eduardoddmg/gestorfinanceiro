{
  "name": "finances-app",
  "version": "1.0.0",
  "main": "backend/src/app.ts",
  "scripts": {
    "start": " cd backend && npm install --save-dev && node build/src/app.ts",
    "dev": "cd backend && nodemon -x ts-node src/app.ts",
    "build": "tsc",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
